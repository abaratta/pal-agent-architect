{
  "name": "pal-architect-telegram",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "trigger dev",
    "deploy": "trigger deploy",
    "vercel:dev": "vercel dev",
    "vercel:deploy": "vercel deploy --prod",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@trigger.dev/sdk": "4.4.6"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/node": "^4.0.0",
    "typescript": "^5.4.0"
  }
  "engines": {
    "node": "22.x"
  }
}